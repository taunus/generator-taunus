{
  "name": "package",
  "version": "1.0.0",
  "scripts": {
    "start": "jadum views/** --output .bin && taunus --output && mkdir -p .bin/public/js && browserify client/js/main.js -do .bin/public/js/all.js && node app"
  },
  "dependencies": {
    "express": "^4.13.1",
    "serve-static": "^1.10.0",
    "taunus": "^7.1.0",
    "taunus-express": "^3.0.0"
  },
  "devDependencies": {
    "browserify": "^10.2.4",
    "jadum": "^1.4.0"
  }
}
