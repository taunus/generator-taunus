language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-taunus' ]; then cd .. && eval "mv $currentfolder generator-taunus" && cd generator-taunus; fi

